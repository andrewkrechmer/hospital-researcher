# Local Postgres.app connection. No schema changes, seeding, or imports.
PGHOST=/tmp
PGDATABASE=hospitals
# PGUSER defaults to your macOS username.
# For hosting, set a Postgres DATABASE_URL instead, using a SELECT-only role.
